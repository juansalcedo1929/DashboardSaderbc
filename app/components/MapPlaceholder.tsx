'use client'
export default function MapPlaceholder() {
  return (
    <div className="bg-card border border-default rounded-lg p-6 h-64 flex items-center justify-center text-neutral">
      <div className="text-center">
        <div className="mb-2 text-xl font-semibold text-primary">Mapa geográfico</div>
        <div className="text-sm">Funcionalidad de zonas por especie (coloreado) pendiente — se implementará por GeoJSON</div>
      </div>
    </div>
  )
}
